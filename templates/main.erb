###############################################################
# This file is managed by puppet. Manual editing of file is   #
# strongly discouraged and might lead to unexpected behaviour!#
###############################################################

@version: 3.3

options {
        chain_hostnames(0);
        time_reopen    (10);
        time_reap      (360);
        log_fifo_size  (4096);
        create_dirs    (yes);
        group          (adm);
        perm           (<%= @perm %>);
        dir_perm       (<%= @dir_perm %>);
        use_dns        (<% if @use_dns  %>yes<% else %>no<% end %>);
        use_fqdn       (<% if @use_fqdn %>yes<% else %>no<% end %>);
	stats_freq     (0);
	bad_hostname   ("^gconfd$");
};

